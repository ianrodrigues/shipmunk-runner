# Shipmunk runner

Standalone execution runner for Shipmunk. The runtime source, independent checks and release packaging are being prepared in the initial implementation pull request.

This repository does not contain the Shipmunk application or operator credentials. No runner release is available yet.
